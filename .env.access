# .env access list
# People granted read access to .env (managed via Linux ACL with setfacl)
# Note: the owner (jnourisa) always has rw access via file ownership — no entry needed here.

skumar     # Saumya Kumar [sku21] — read access
