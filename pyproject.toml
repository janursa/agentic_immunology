[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-immunology"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["openai>=1.0.0"]

[tool.setuptools.packages.find]
where = [".."]
include = ["agentic_immunology*"]
